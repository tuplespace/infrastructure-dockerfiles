version: 2.1

jobs:
  build:
    docker:
      - image: docker:18.06.3-ce-git
    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: build-base image
          command: |
            cd base-build
            docker build . -t tuplestream/base-build


workflows:
  version: 2
  build_and_test:
    jobs:
      - build
